# Content Hub Docs

Living documentation. Updated alongside code changes — single source of truth.

## Documents

- [**Content Workflow**](/docs/content-workflow) — How content gets made: idea → script → CapCut → animations → ready → posted. The human creation flow.
- [**Content Hub Architecture**](/docs/content-hub) — How the app works: tables, API routes, R2 layout, integrations, env vars. Read at session start.

## Convention

Every tool under `tools/<app>/` keeps its docs at `tools/<app>/docs/` and exposes them at `/docs` inside the app. Edit the markdown next to the code; the docs page auto-renders on next request.
