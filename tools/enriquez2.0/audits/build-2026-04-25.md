# BUILD Audit — 2026-04-25

## Top gaps (impact × ease)

1. **`tools/content-hub/content_hub.db` is committed to git — a live database in version control** — Standard requires no silent data corruption risks; `git_tracked.suspicious_tracked` explicitly flags `tools/content-hub/content_hub.db` as tracked. Database files change on every write, causing noisy diffs and risk of leaking content state.
   Fix: Add `tools/content-hub/content_hub.db` to `.gitignore` (alongside the already-ignored `tools/dashboard/dashboard.db`), then `git rm --cached` it.
   Impact 4 × Ease 5 = **20**.

2. **No `services/` folder structure matches the standard's `projects/eps/`, `projects/personal/`, `tools/<domain>/` layout** — Standard mandates one repo with domain folders; `services/ryan` (21 files, 16 `.py`) exists as a top-level `services/` tree sitting outside the `projects/` or `tools/` hierarchy, creating a fourth root-level work tree with no documented home.
   Fix: Move `services/ryan/` into `projects/personal/clients/ryan/` (which already exists with 19 files) and delete the `services/` root dir.
   Impact 4 × Ease 4 = **16**.

3. **`tools/archive` has 10 uncommitted `.py` files with no README or purpose marker** — Standard expects atomic, verifiable commits and named domain folders; `tools/archive` shows 10 `.py` files on disk but zero matching tracked files under `git_tracked.byTopDir["tools"]` attribution suggests they're gitignored or orphaned, with no signal about whether they're live, deprecated, or safe to delete.
   Fix: Add a one-line `README.md` to `tools/archive/` stating what's archived and why, then commit or delete.
   Impact 3 × Ease 5 = **15**.

4. **CLAUDE.md is only 82 lines — too thin to enforce token-discipline and skill routing at scale** — Standard requires "read only what's needed, CONTEXT.md + one workflow"; at 82 lines / 4903 bytes, CLAUDE.md is the single context anchor for 22 skills and 23 automations with no verified domain CONTEXT.md files under `projects/eps/` or `projects/personal/` visible in tracked counts.
   Fix: Add `projects/eps/CONTEXT.md` and `projects/personal/CONTEXT.md` as lean (≤50 line) domain briefs; link them from CLAUDE.md.
   Impact 4 × Ease 3 = **12**.

5. **23 LaunchAgent plists tracked in git but no health/status log is committed** — Standard says "fail loud, never silent"; `automation/` has 23 `.plist` files tracked (`git_tracked.byTopDir["automation"] = 35`) but `com.enriquezOS.automation-status.plist` exists as the only status checker — no committed log file or last-run output file is visible in tracked state to confirm automations are alive.
   Fix: Add a `automation/STATUS.md` (auto-written by `automation-status` plist) to `.gitignore` but verify it's actually being written to disk on each run.
   Impact 4 × Ease 3 = **12**.

6. **`projects/personal/clients/ryan/` and `services/ryan/` are duplicate client workspaces for the same person** — Standard is one repo, clean domain folders; `projects/personal/clients/ryan` (19 files) and `services/ryan` (21 files) both exist and both appear tracked (`git_tracked.byTopDir["services"] = 26`, `git_tracked.byTopDir["projects"] = 645`), meaning Ryan's work is split across two locations with no clear canonical home.
   Fix: Audit which folder is active, consolidate into `projects/personal/clients/ryan/`, delete the other, single commit.
   Impact 3 × Ease 4 = **12**.

7. **`q2-2026.md` has no 30-day goal or weekly target filled in** — Standard's Q2 anchor is "first paid client by June + 10k followers"; both the `## 30-Day Goal` and `## This Week` fields are literally "TBD — Allen fills." No build work can be prioritized against an empty filter.
   Fix: Fill `q2-2026.md` 30-day goal and this-week fields now — takes 5 minutes, unblocks every session's prioritization.
   Impact 5 × Ease 5 = **25** *(ranked here because it's a people/process gap not a repo gap, but impact is highest).*

---

*(Reordered by score:)*

| Rank | Score | Gap |
|------|-------|-----|
| 1 | 25 | q2-2026.md empty |
| 2 | 20 | content_hub.db tracked |
| 3 | 16 | services/ryan outside standard layout |
| 4 | 15 | tools/archive orphaned |
| 5 | 12 | CLAUDE.md / no domain CONTEXT.md |
| 6 | 12 | automation health not logged |
| 7 | 12 | duplicate ryan workspaces |

---

## Quick wins (today)
- **Untrack `tools/content-hub/content_hub.db`** — one `git rm --cached` + `.gitignore` line, done in 2 minutes.
- **Fill `q2-2026.md`** — 30-day goal + this-week fields. Zero build required, pure unlock.
- **Add `README.md` to `tools/archive/`** — one file, one commit, clears ambiguity on 10 orphaned `.py` files.
- **Consolidate `services/ryan/` → `projects/personal/clients/ryan/`** — removes a structural anomaly, one `git mv` + commit.

## Deferred (low priority)
- **`non-negotiables.md` still has seed candidates, not confirmed rules** — real gap but it's identity/ops, not build domain; defer to an IDENTITY audit.
- **`tools/dashboard` has `.db-shm`/`.db-wal` files on disk** — already covered by `*.db-shm`/`*.db-wal` gitignore patterns; correctly ignored, not a tracking gap.
- **Render frame directories (2303 + 1862 + 1260 `.png` files)** — all covered by `*.png` gitignore; correctly excluded, no action needed.
- **`second-brain` package has no `top_dirs` entry** — suggests it's either empty or fully gitignored; low signal to act on without more state.