# BUILD Audit — 2026-04-25

## Top gaps (impact × ease)

1. **`tools/content-hub/content_hub.db` is tracked in git — live database committed to repo** — Standard says "fail loud, never silent" and "local over API"; committing a live SQLite database risks leaking post data, credentials stored in DB, and creating merge conflicts. `git_tracked.suspicious_tracked` explicitly flags `tools/content-hub/content_hub.db`.
   Fix: Add `tools/content-hub/content_hub.db` to `.gitignore` (pattern `tools/content-hub/*.db`) and run `git rm --cached tools/content-hub/content_hub.db`.
   Impact 5 × Ease 5 = 25.

2. **`schedule_next_week` is empty — no content scheduled beyond 2026-04-23** — Standard requires systems that run themselves; Allen's Q2 anchor is 10k followers via 2 reels + 2 carousels + 1 long-form/week. `content-hub.json` shows `schedule_next_week: []` with the week of 2026-04-28 entirely unplanned despite 20+ ideas in `active_ideas` with status `new`.
   Fix: Run the `content` skill to slot the week of 2026-04-28 from the existing `active_ideas` backlog right now.
   Impact 5 × Ease 5 = 25.

3. **All 25 posted items show zero views/likes/comments — no engagement data is being pulled** — Standard principle "fail loud, never silent" means a broken metric loop should surface, not silently return zeroes. Every row in `posts_last_30d` has `views: 0, likes: 0, comments: 0, shares: 0`, including posts from 2026-04-20 that have had 5 days to accumulate data.
   Fix: Audit the `com.enriquezOS.creator-feed.plist` automation to confirm it is loaded and the platform API tokens (IG, YouTube) are valid.
   Impact 5 × Ease 4 = 20.

4. **`tools/shared` contains `.pem` files that are git-tracked** — Standard says "local over API, cost ≈ $0 default" and "fail loud"; `.gitignore` already has a `*.pem` pattern, but `git_tracked.byTopDir` shows `tools` has 391 tracked files, and `top_dirs` lists `tools/shared` with 2 `.pem` files — if these slipped through before the ignore pattern was added they remain tracked. Confirm via `git ls-files tools/shared/*.pem`.
   Fix: Run `git ls-files tools/shared/*.pem`; if any appear, run `git rm --cached` on each immediately.
   Impact 5 × Ease 4 = 20.

5. **`q2-2026.md` 30-day goal and this-week fields are blank — no operational anchor exists** — Standard requires every session to move something forward against a defined target; both `## 30-Day Goal` and `## This Week` are marked `TBD — Allen fills`. Without these, the `start` skill and `wrap` skill have nothing concrete to push against.
   Fix: Fill `q2-2026.md` 30-day goal (e.g., "3 free consulting clients by 2026-05-25") and this-week tasks in one 5-minute session.
   Impact 4 × Ease 5 = 20.

6. **`services/ryan` and `projects/personal/clients/ryan` are duplicate structures for the same entity** — Standard says one repo with clean domain folders (`projects/`, `tools/`); two separate top-level paths (`services/ryan` with 21 files and `projects/personal/clients/ryan` with 19 files) serve the same apparent client, creating split context that wastes tokens and risks version drift.
   Fix: Decide canonical location (`services/ryan` or `projects/personal/clients/ryan`), move files, delete the other, update any skill paths that reference it.
   Impact 3 × Ease 4 = 12.

7. **`non-negotiables.md` is unfilled — QA gate and outreach rules exist only as seed candidates** — Standard's "fail loud, never silent" and values priority "Less Allen Input > Accuracy" depend on hard rules being encoded. The file explicitly says `TBD — Allen fills first session`; the consultancy's first free client deliverables are imminent and need a committed QA gate before that happens.
   Fix: Promote the five seed candidates from `non-negotiables.md` to locked rules in one session; delete the `TBD` line.
   Impact 3 × Ease 5 = 15.

---

## Quick wins (today)
- **Remove `content_hub.db` from git tracking** — `git rm --cached tools/content-hub/content_hub.db` + add pattern to `.gitignore`. 5 minutes, removes the only flagged suspicious tracked file.
- **Slot next week's content** — Use the `content` skill with the 20+ `active_ideas` already in the DB; `schedule_next_week` is completely empty right now.
- **Fill `q2-2026.md`** — 30-day goal and this-week are both `TBD`; takes 5 minutes and unblocks every session brief from today forward.
- **Promote `non-negotiables.md` seed candidates** — Five rules already written, just need Allen to confirm and delete the `TBD` line.

## Deferred (low priority)
- **`tools/archive` (10 `.py` files)** — Cannot determine if these are dead code without file contents; low risk, defer until a cleanup sprint.
- **Hyperframes student-kit reference files** — Large HTML composition trees under `projects/personal/reference/hyperframes-student-kit` are gitignored-candidate noise but show zero tracking issues; cosmetic, not blocking.
- **`automation/` having only 1 `.md` for 23 plists** — Documentation gap is real but automations are running; address in a docs sprint after Q2 anchor is stable.