# SERVE Audit — 2026-04-25

## Top gaps (impact × ease)

1. **No offer document exists — pricing, scope, and delivery SOP live nowhere in the repo** — Standard requires a defined offer ($2K-$8K setup + $500-$1K/mo retainer, discovery → build → QA → handoff). No file under `projects/personal/sales/` or anywhere else codifies this. The landing page folder (`projects/personal/sales/landing-page/`) has 9 files but no offer/pricing doc.
   Fix: Create `projects/personal/sales/offer.md` with scope tiers, pricing, and 4-step delivery SOP.
   Impact 5 × Ease 5 = 25.

2. **`content-hub.db` is empty (0 bytes) — personal CRM has no lead data to convert** — Standard says audience turns into paid clients; the `personal-crm` skill and `com.enriquezOS.personal-crm-cleanup.plist` automation exist, but `content-hub.db` (path: `tools/content-hub/content-hub.db`) is 0 bytes, meaning no leads are being tracked or nurtured toward the first paid client.
   Fix: Run `pnpm seed` in `tools/content-hub/` and manually enter the current warm leads from outreach templates.
   Impact 5 × Ease 4 = 20.

3. **Outreach templates exist but no qualifying criteria or CTA script tied to the soft offer** — Standard specifies the soft CTA: "Everything is free. If you don't have time, I'll set it up for you." The 12 `.md` files in `projects/personal/outreach/templates/` exist, but none are confirmed to carry this specific CTA or a qualifying filter for trades/real estate/coaching niche.
   Fix: Add a `qualifying-cta.md` to `projects/personal/outreach/templates/` with the soft CTA and 3 qualifying questions.
   Impact 4 × Ease 5 = 20.

4. **`services/ryan` exists as a proto-client folder but no onboarding SOP or QA gate document is present** — Standard mandates scope discovery → build → QA gate → handoff, and "never ship without QA." `services/ryan/` has 21 files (16 `.py`, 1 `.md`) but the single `.md` file is unconfirmed as a QA gate or delivery checklist.
   Fix: Create `services/ryan/DELIVERY-CHECKLIST.md` with the 4-step SOP and QA sign-off fields as a reusable template.
   Impact 4 × Ease 4 = 16.

5. **No testimonial capture mechanism exists for the first 3-5 free clients** — Standard explicitly calls for first 3-5 clients free for testimonials, but there is no form, template, or automation (none of the 23 `.plist` automations is named for testimonial capture) to collect or store social proof post-delivery.
   Fix: Add `projects/personal/sales/testimonial-request.md` with a 3-question async testimonial script to send post-handoff.
   Impact 4 × Ease 4 = 16.

6. **`ph-outreach-daily.plist` and `ph-outreach-discover.plist` run without a documented "real reason to contact" filter** — Non-negotiables state "no cold outreach without a real reason to contact." Both automations (`automation/com.enriquezOS.ph-outreach-daily.plist`, `automation/com.enriquezOS.ph-outreach-discover.plist`) are live with no visible trigger-qualification doc tied to them.
   Fix: Add a `trigger-criteria.md` in `projects/personal/outreach/` listing valid outreach triggers before each automation fires.
   Impact 3 × Ease 5 = 15.

7. **Landing page exists but no scope-confirmation step before any paid API usage** — Standard and non-negotiables both require scope confirmation before paid API calls. `projects/personal/sales/landing-page/` has 5 `.html` files and a `.js` file but no visible intake form or scope-gate step that would prevent premature API spend on unqualified leads.
   Fix: Add a scope-confirmation intake step (even a simple Google Form link) to the landing page before any automation triggers.
   Impact 3 × Ease 4 = 12.

---

## Quick wins (today)
- **Create `projects/personal/sales/offer.md`** — 30 minutes, no tooling needed. Pricing and SOP are already known; just needs to be written down. Directly unblocks first paid client conversation.
- **Add `qualifying-cta.md` to `projects/personal/outreach/templates/`** — Copy the soft CTA from standard.md, add 3 qualifying questions. Done in 15 minutes.
- **Add `testimonial-request.md` to `projects/personal/sales/`** — Write the 3-question async script now, before you have clients, so it's ready when Ryan or the next free client finishes.

---

## Deferred (low priority)
- **SERVE `standard.md` patterns/anti-patterns sections** — Both are stub placeholders pending `pnpm distill --domain serve`. Low priority until at least one client delivery is complete and there's real data to distill.
- **`content-hub.db` schema review** — DB is 0 bytes but the migration scripts exist (`pnpm migrate`). Deprioritised because seeding with real leads (gap #2) is the higher-value action first.