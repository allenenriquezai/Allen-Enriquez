# Web CRM for Personal Brand

**Status:** Parked — build when usage resets

**What:** Clean, minimal web UI on top of existing Google Sheets CRM.
- Flask + vanilla HTML/CSS/JS
- Pipeline/kanban view by call outcome
- Contact cards, inline editing, search/filter
- Reads/writes directly to existing Sheet (no migration)
- Runs locally, no auth needed

**Estimate:** 3-4 sessions, ~$15-25 in tokens

**Why wait:** Sheet workflow is working. Priority is making calls + outreach.
